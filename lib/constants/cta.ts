import type { CTAType } from "@/types";

export const CTA_LABELS: Readonly<Record<CTAType, string>> = {
  buy_now: "Buy now",
  add_to_cart: "Add to cart",
  download_free: "Download for free",
  preorder: "Pre-order",
  join_waitlist: "Join waitlist",
};
