# features/

Create one folder per domain area, for example:

- `features/auth`
- `features/listings`
- `features/orders`
- `features/profile`

Each feature can own its components, hooks, services, and validation logic.
